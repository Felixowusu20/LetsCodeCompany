import { aboutMock } from "../mockData";

/** ISO date shown on legal pages and in exported Markdown. */
export const LEGAL_LAST_UPDATED = "2026-05-10";

export const LEGAL_ENTITY = aboutMock.company;
